import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jakeroyston.japan2026',
  appName: 'Japan 2026',
  webDir: 'www',
  ios: {
    contentInset: 'always',
  },
};

export default config;

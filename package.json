{
  "name": "japan-trip-ios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open": "cap open ios",
    "build": "cap sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
